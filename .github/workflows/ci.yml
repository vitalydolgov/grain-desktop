name: CI

on:
  push:
    branches: ["claude/**"]
    paths:
      - "Sources/**"
      - "project.yml"
      - ".github/workflows/ci.yml"

jobs:
  build:
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: |
          # project.yml includes local.yml (developer-only, gitignored);
          # provide an empty one so the include resolves on CI.
          echo '{}' > local.yml
          xcodegen generate

      - name: Build macOS app
        run: |
          set -o pipefail
          xcodebuild build \
            -project GrainApp.xcodeproj \
            -scheme GrainDesktop \
            -destination "generic/platform=macOS" \
            CODE_SIGNING_ALLOWED=NO \
            -skipMacroValidation \
            | xcbeautify --quiet

      - name: Build iOS app
        run: |
          set -o pipefail
          xcodebuild build \
            -project GrainApp.xcodeproj \
            -scheme GrainPhone \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGNING_ALLOWED=NO \
            -skipMacroValidation \
            | xcbeautify --quiet
